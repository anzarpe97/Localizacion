<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- 
        Agregamos los campos 'ref' y 'ref_unit' en las líneas de compra (purchase.order.line)
        Estos campos se mostrarán en el formulario de purchase.order en las líneas
    -->
    <record id="view_purchase_order_form_inherit_ref" model="ir.ui.view">
        <field name="name">purchase.order.form.ref.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <!-- Agregamos los campos después de price_unit en el tree de order_line -->
            <xpath expr="//field[@name='order_line']//field[@name='price_unit']" position="after">
                <field name="ref_unit" string="REF Unit" optional="show"/>
            </xpath>
            <!-- Agregamos ref después de price_subtotal -->
            <xpath expr="//field[@name='order_line']//field[@name='price_subtotal']" position="after">
                <field name="ref" string="REF Subtotal" optional="show"/>
            </xpath>
        </field>
    </record>
</odoo>