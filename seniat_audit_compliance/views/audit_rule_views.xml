<odoo>
    <record id="view_audit_rule_tree" model="ir.ui.view">
        <field name="name">audit.rule.tree</field>
        <field name="model">audit.rule</field>
        <field name="arch" type="xml">
            <tree editable="bottom">
                <field name="model_id" options="{'no_create': True, 'no_open': True}"/>
                <field name="name" string="Nombre Técnico"/>
                <field name="active" widget="boolean_toggle"/>
            </tree>
        </field>
    </record>

    <record id="view_audit_rule_form" model="ir.ui.view">
        <field name="name">audit.rule.form</field>
        <field name="model">audit.rule</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <widget name="web_ribbon" title="Archivado" bg_color="bg-danger" invisible="active"/>
                    <group>
                        <group>
                            <field name="model_id" options="{'no_create': True}"/>
                            <field name="active" widget="boolean_toggle"/>
                        </group>
                        <group>
                            <div class="alert alert-info" role="alert">
                                Selecciona el modelo que deseas auditar. 
                                Al activar esta regla, se interceptarán automáticamente las operaciones 
                                de Creación, Escritura y Eliminación.
                            </div>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_audit_rule_search" model="ir.ui.view">
        <field name="name">audit.rule.search</field>
        <field name="model">audit.rule</field>
        <field name="arch" type="xml">
            <search>
                <field name="model_id"/>
                <filter string="Activos" name="active" domain="[('active', '=', True)]"/>
                <filter string="Inactivos" name="inactive" domain="[('active', '=', False)]"/>
            </search>
        </field>
    </record>

    <record id="action_audit_rule" model="ir.actions.act_window">
        <field name="name">Reglas de Auditoría</field>
        <field name="res_model">audit.rule</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Define tu primera regla de auditoría
            </p>
            <p>
                Selecciona los modelos de Odoo (ej. sale.order, account.move) que requieren 
                trazabilidad estricta para el SENIAT.
            </p>
        </field>
    </record>
</odoo>